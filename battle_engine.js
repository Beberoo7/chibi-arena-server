'use strict';
const vm=require('vm');
const ENGINE_SOURCE="\nconst CHARS={\n dragon:{id:'dragon',name:'Demi-Dragon',role:'BRUISER',color:'#ff555f',maxHp:1280,armor:32,speed:72,\n  skill1:{name:'Griffe fracturante',cd:0,target:'enemy',desc:'135 dégâts. Retire 6 Armure pendant 2 manches, cumulable jusqu’à -18.'},\n  skill2:{name:'Souffle écarlate',cd:1,target:'enemy',desc:'150 dégâts à la cible, 80 à l’autre ennemi et Brûlure x1 pendant 3 manches.'},\n  skill3:{name:'Forme Dragon',cd:2,target:'self',desc:'3 manches : +30 % dégâts, +10 Vitesse et -20 % dégâts reçus.'}},\n rogue:{id:'rogue',name:'Voleur Poison',role:'ASSASSIN',color:'#63ef66',maxHp:880,armor:14,speed:122,\n  skill1:{name:'Lame infectée',cd:0,target:'enemy',desc:'105 dégâts + Poison x1 pendant 3 manches. Si la cible est déjà empoisonnée, ajoute 1 Poison supplémentaire.'},\n  skill2:{name:'Disparition toxique',cd:2,target:'self',desc:'Réduit de 50 % les dégâts de la prochaine attaque ciblée subie, +25 Vitesse, prochain dégât critique et applique Poison x1 aux deux ennemis vivants.'},\n  skill3:{name:'Exécution toxique',cd:3,target:'enemy',desc:'130 dégâts + 45 par Poison consommé. 60 % anti-soin pendant 2 manches puis remet Poison x2.'}},\n wolf:{id:'wolf',name:'Loup sanguinaire',role:'DPS',color:'#ff6670',maxHp:1040,armor:22,speed:103,\n  skill1:{name:'Morsure ouverte',cd:0,target:'enemy',desc:'145 dégâts + Saignement x1 pendant 3 manches et rend 10 % des dégâts infligés.'},\n  skill2:{name:'Frénésie sanguinaire',cd:1,target:'self',desc:'2 manches : +35 % dégâts, +20 Vitesse et 25 % vol de vie.'},\n  skill3:{name:'Éventration',cd:2,target:'enemy',desc:'180 dégâts + 55 par Saignement consommé. +10 % dégâts si la cible est sous 35 % PV.'}},\n priest:{id:'priest',name:'Prêtre du Châtiment',role:'SUPPORT / CONTRÔLE',color:'#ffd86f',maxHp:980,armor:19,speed:88,\n  skill1:{name:'Éclat rédempteur',cd:0,target:'enemy',desc:'125 dégâts à une cible et soigne légèrement l’allié avec le moins de PV (70 PV).'},\n  skill2:{name:'Silence sacré',cd:4,target:'enemy',desc:'Réduit un ennemi au silence. Cette compétence reste prioritaire sur les actions normales.'},\n  skill3:{name:'Liturgie d’abstinence',cd:6,target:'self',desc:'Pendant 2 tours, chaque allié récupère 12 % de ses PV max par tour. Réduit les soins ennemis de 50 % pendant 3 tours.'}},\n sniper:{id:'sniper',name:'Chasseur Sniper',role:'BURSTER',color:'#74aaff',maxHp:820,armor:12,speed:96,\n  skill1:{name:'Tir précis',cd:0,target:'enemy',desc:'115 dégâts. Si la cible a déjà l’armure brisée, inflige 25 dégâts bonus.'},\n  skill2:{name:'Tir perforant',cd:2,target:'enemy',desc:'210 dégâts et Armure -7 pendant 2 manches.'},\n  skill3:{name:'Balle de jugement',cd:4,target:'enemy',desc:'325 dégâts. Inflige 18 % de dégâts bonus aux cibles au-dessus de 70 % PV.'}},\n shadow:{id:'shadow',name:'Démoniste Ombre',role:'DPS / VOL DE VIE',color:'#c07aff',maxHp:990,armor:18,speed:94,\n  skill1:{name:'Toucher nécrotique',cd:0,target:'enemy',desc:'125 dégâts et rend 45 % des dégâts infligés.'},\n  skill2:{name:'Pacte d’ombre',cd:2,target:'self',desc:'2 manches : +35 % dégâts, +25 Vitesse et 50 % vol de vie.'},\n  skill3:{name:'Voile de la mort',cd:3,target:'enemy',desc:'210 dégâts à la cible, 120 à l’autre ennemi, soigne selon les dégâts infligés et gagne 100 Bouclier.'}}\n,\n guardian:{id:'guardian',name:'Guerrier Protection',role:'TANK / PROTECTION',color:'#7db2ff',maxHp:1280,armor:34,speed:68,\n  skill1:{name:'Entaille circulaire',cd:0,target:'enemy',desc:'95 dégâts à la cible et 95 à l’autre ennemi.'},\n  skill2:{name:'Mur protecteur',cd:2,target:'ally',desc:'PRIORITÉ ABSOLUE. Protège un allié de la prochaine attaque ciblée et lui donne 80 Bouclier.'},\n  skill3:{name:'Rempart miroir',cd:3,target:'self',desc:'PRIORITÉ ABSOLUE. Renvoie la prochaine attaque ciblée subie à 150 % des dégâts reçus.'}},\n berserker:{id:'berserker',name:'Berserker Écarlate',role:'DPS / EXÉCUTION',color:'#ff6767',maxHp:1040,armor:20,speed:98,\n  skill1:{name:'Frappe vorace',cd:0,target:'enemy',desc:'95 dégâts et rend légèrement des PV au berserker.'},\n  skill2:{name:'Rage du manque',cd:2,target:'enemy',desc:'135 dégâts, augmentés jusqu’à +70 % selon les PV manquants.'},\n  skill3:{name:'Dette de sang',cd:3,target:'enemy',desc:'Inflige ses PV manquants en dégâts, plafonnés à 650. Cette attaque ne critique plus.'}}\n, augment:{id:'augment',name:'Mage Augmentation',role:'SUPPORT / AMPLIFICATION',color:'#b884ff',maxHp:980,armor:19,speed:106,\n  skill1:{name:'Orbe cumulative',cd:0,target:'enemy',desc:'125 dégâts. Ce sort gagne +35 dégâts à chaque utilisation, jusqu’à +140.'},\n  skill2:{name:'Accélération runique',cd:1,target:'ally',desc:'Renforce l’allié : +60 % dégâts, +28 Vitesse pendant 2 manches et 90 Bouclier.'},\n  skill3:{name:'Scellement miroir',cd:4,target:'enemy',desc:'PRIORITÉ ÉLEVÉE. Retire instantanément 35 % des PV actuels de la cible. Ignore l’armure et les boucliers.'}},\n dragonette:{id:'dragonette',name:'Dragonette',role:'SUPPORT / BRÛLURE',color:'#71ef93',maxHp:950,armor:18,speed:87,\n  skill1:{name:'Flamme draconique',cd:0,target:'enemy',desc:'105 dégâts et Brûlure x1 pendant 3 manches.'},\n  skill2:{name:'Braise curative',cd:2,target:'ally',desc:'Soigne un allié de 135 PV et applique Brûlure x1 à l’ennemi le plus affaibli.'},\n  skill3:{name:'Inferno vital',cd:5,target:'enemy',desc:'Applique Brûlure x4 à un ennemi et soigne fortement l’allié le plus blessé.'}}\n, plague:{id:'plague',name:'Démoniste Maladie',role:'DOTEUR / AFFLICTION',color:'#9cff6e',maxHp:920,armor:16,speed:92,\n  skill1:{name:'Peste hématophage',cd:0,target:'enemy',desc:'80 dégâts directs, Maladie x2 pendant 3 manches et anti-soin 35 % pendant 2 manches.'},\n  skill2:{name:'Fièvre flétrissante',cd:2,target:'enemy',desc:'100 dégâts directs, Maladie x2 pendant 3 manches et Vitesse -15 pendant 2 manches.'},\n  skill3:{name:'Pandémie terminale',cd:4,target:'enemy',desc:'120 dégâts directs, Attaque -18 %, applique les autres maladies et ajoute Maladie x2. Long cooldown.'}}\n, dice:{id:'dice',name:'Voleur Dé',role:'ASSASSIN / CHANCE',color:'#ff8e7a',maxHp:930,armor:15,speed:116,\n  skill1:{name:'Lancer de dés',cd:0,target:'enemy',desc:'Lance un dé de 1 à 6 : 90 / 110 / 135 / 165 / 210 / 270 dégâts selon le résultat.'},\n  skill2:{name:'Jackpot sanglant',cd:2,target:'enemy',desc:'55 % de chance de critique. 215 dégâts de base en critique + Saignement x2 ; sinon 145 dégâts.'},\n  skill3:{name:'Tout ou rien',cd:4,target:'enemy',desc:'10 % de chance d’infliger 3000 dégâts. En cas d’échec, inflige désormais 220 dégâts.'}}\n\n\n,\n paladin:{id:'paladin',name:'Paladin',role:'TANK / LUMIÈRE',color:'#ffd76e',maxHp:1240,armor:30,speed:82,\n  skill1:{name:'Lame sacrée',cd:0,target:'enemy',desc:'150 dégâts à une cible puis gagne un bouclier égal à 55 % des dégâts infligés.'},\n  skill2:{name:'Prière de rempart',cd:2,target:'self',desc:'Se soigne selon ses PV manquants. Plus il est blessé, plus le soin est puissant.'},\n  skill3:{name:'Défi rayonnant',cd:4,target:'self',desc:'Provoque les ennemis : ils sont forcés de l’attaquer ce tour-ci et le suivant.'}},\n samurai:{id:'samurai',name:'Samouraï des Ombres',role:'DPS / CONTRE',color:'#7f92ff',maxHp:960,armor:16,speed:111,\n  skill1:{name:'Lame du crépuscule',cd:0,target:'enemy',desc:'Frappe une cible. Les dégâts augmentent selon les PV manquants de l’ennemi.'},\n  skill2:{name:'Contre lunaire',cd:2,target:'self',desc:'Se met en garde. Si elle subit une attaque ciblée avant la fin de la manche, elle réduit cette attaque de moitié et contre-attaque.'},\n  skill3:{name:'Déferlante des ombres',cd:4,target:'enemy',desc:'Frappe 3 fois aléatoirement les ennemis, avec des dégâts basés sur leurs PV max.'}},\n shaman:{id:'shaman',name:'Chaman Élémentaire',role:'MAGE / ÉLÉMENTS',color:'#69d6ff',maxHp:940,armor:17,speed:96,\n  skill1:{name:'Éclair ancestral',cd:0,target:'enemy',desc:'100 dégâts et 15 % de chance de paralyser : la prochaine attaque de la cible inflige 50 % de dégâts en moins.'},\n  skill2:{name:'Orbe de magma',cd:2,target:'enemy',desc:'Frappe critique, 105 dégâts de base et Brûlure x1 à 16 dégâts pendant 3 manches.'},\n  skill3:{name:'Maelstrom',cd:4,target:'enemy',desc:'Déchaîne 3 éclairs (65 dégâts, 15 % paralysie chacun) et 2 orbes de magma critiques (76 dégâts + Brûlure) sur des ennemis aléatoires.'}}\n,\n bard:{id:'bard',name:'Barde de Guerre',role:'SUPPORT / PROTECTION',color:'#ffbf66',maxHp:970,armor:18,speed:104,\n  skill1:{name:'Note percutante',cd:0,target:'enemy',desc:'90 dégâts. La prochaine action de la cible inflige 25 % de dégâts directs en moins.'},\n  skill2:{name:'Hymne protecteur',cd:2,target:'ally',desc:'Donne 100 Bouclier à un allié et retire complètement 1 effet négatif.'},\n  skill3:{name:'Concert symphonique',cd:5,target:'self',desc:'PRIORITÉ ÉLEVÉE. Cette manche, le premier coup direct reçu par chaque allié est annulé et 60 % des dégâts prévus sont convertis en soins.'}}\n\n,\n valkyrie:{id:'valkyrie',name:'Valkyrie de Givre',role:'TANK / CONTRÔLE',color:'#8fd7ff',maxHp:1320,armor:35,speed:80,\n  skill1:{name:'Coup de bouclier',cd:0,target:'enemy',desc:'105 dégâts et réduit la Vitesse de 30 de l’ennemi pour sa prochaine action.'},\n  skill2:{name:'Défi glacé',cd:2,target:'enemy',desc:'Force un ennemi à cibler la Valkyrie avec sa prochaine compétence offensive et réduit les dégâts de cette action de 25 %.'},\n  skill3:{name:'Gel absolu',cd:6,target:'self',desc:'PRIORITÉ ABSOLUE. Gèle tous les autres combattants pour cette manche : leurs actions validées sont annulées.'}}\n\n,\n\n monk:{id:'monk',name:'Moine du Ki',role:'BRUISER / KI',color:'#f0bf62',maxHp:1000,armor:18,speed:108,\n  passive:'Chaque technique prépare un Ki pour la prochaine compétence. TIGRE : +20 % dégâts et Armure -5 avant l’impact. GRUE : +50 Bouclier et -20 % sur le prochain coup ciblé reçu. SERPENT : soigne 60 PV + 10 % des PV manquants et réduit de 20 % les dégâts de la prochaine action de la cible.',\n  skill1:{name:'Paume du Tigre',cd:0,target:'enemy',desc:'120 dégâts. Active le Ki du Tigre : la prochaine compétence inflige +20 % dégâts et applique Armure -5 avant l’impact pendant 2 manches.'},\n  skill2:{name:'Pas de la Grue',cd:2,target:'enemy',desc:'145 dégâts. Active le Ki de la Grue : la prochaine compétence donne 50 Bouclier et réduit de 20 % le prochain coup ciblé reçu.'},\n  skill3:{name:'Frappe du Serpent',cd:3,target:'enemy',desc:'195 dégâts. Active le Ki du Serpent : la prochaine compétence soigne 60 PV + 10 % des PV manquants et réduit de 20 % les dégâts de la prochaine action de la cible.'}}\n,\n archangel:{id:'archangel',name:'Archange des Cieux',role:'DPS / JUGEMENT',color:'#ffe39a',maxHp:870,armor:13,speed:102,\n  skill1:{name:'Marque du Jugement',cd:0,target:'enemy',desc:'120 dégâts et applique une Marque divine : le prochain dégât direct reçu par la cible est obligatoirement critique.'},\n  skill2:{name:'Éblouissement céleste',cd:2,target:'enemy',desc:'95 dégâts. La prochaine compétence ciblée utilisée par cet ennemi vise un combattant vivant aléatoire parmi les 4.'},\n  skill3:{name:'Rayon du Jugement',cd:2,target:'enemy',desc:'175 dégâts et charge l’Œil divin. À la 3e utilisation, la cible est éliminée instantanément puis le compteur repart à 0/3.'}}\n,\n voidknight:{id:'voidknight',name:'Épéiste du Néant',role:'DPS / CONDAMNÉ',color:'#b66cff',maxHp:920,armor:13,speed:108,\n  passive:'Commence avec 5 sceaux. Chaque compétence consommée retire 1 sceau. Après la 5e, la compétence se résout puis l’Épéiste meurt immédiatement, même avec PV ou bouclier.',\n  skill1:{name:'Entaille du Néant',cd:0,target:'enemy',desc:'210 dégâts. Une frappe brute monstrueuse, sans condition.'},\n  skill2:{name:'Fracture dimensionnelle',cd:2,target:'enemy',desc:'340 dégâts et ignore 50 % du Bouclier de la cible.'},\n  skill3:{name:'Exécution du Néant',cd:3,target:'enemy',desc:'520 dégâts. Une frappe d’exécution colossale, sans condition ni hasard.'}}\n};\nlet myTeam=[],botTeam=[],round=1,phase='planning',playerPlans={},botPlans={},ended=false;\nlet matchStats={},matchLog=[],currentStatsActor=null,roundFreezeSourceKey=null;\nfunction logBattle(msg,type=''){matchLog.push({round,type,msg})}\nfunction floatText(){} function flash(){} function anim(){} function hitAnim(){} function divineJudgmentFX(){} function showAbsoluteFreeze(){} function updateUI(){}\nfunction statKey(f){return f?`${f.side}-${f.index}`:''}\n\nfunction fighterByStatKey(k){if(!k)return null;const [side,idx]=k.split('-');return getFighter(side,Number(idx))}\n\nfunction recordDamage(source,target,amount){const a=Math.max(0,Number(amount)||0);if(a<=0||!target)return;const ts=matchStats[statKey(target)];if(ts)ts.taken+=a;if(source){const ss=matchStats[statKey(source)];if(ss)ss.damage+=a}}\n\nfunction recordDamageByKey(sourceKey,target,amount){const a=Math.max(0,Number(amount)||0);if(a<=0||!target)return;const ts=matchStats[statKey(target)];if(ts)ts.taken+=a;const ss=matchStats[sourceKey];if(ss)ss.damage+=a}\n\nfunction recordHealing(source,target,amount){const a=Math.max(0,Number(amount)||0);if(a<=0)return;const ss=source?matchStats[statKey(source)]:null;if(ss)ss.healing+=a}\n\nfunction makeFighter(id,side,index){const c=CHARS[id];return {...c,side,index,hp:c.maxHp,shield:0,alive:true,cd1:0,cd2:0,cd3:0,poisonStacks:[],diseaseStacks:[],burnStacks:[],bleedStacks:[],armorBreak:0,armorBreakT:0,antiHeal:.0,antiHealT:0,hpSealT:0,dr:0,drT:0,dmgBuff:0,dmgBuffT:0,speedBuff:0,speedBuffT:0,lifesteal:0,lifestealT:0,arcaneCharge:0,dodgeNext:false,halfNextTargeted:false,nextCrit:false,silencedRound:false,guardBy:null,guardT:0,reflectNext:0,reflectT:0,sanctuaryT:0,sanctuaryPower:0,sanctuarySourceKey:null,tauntSource:null,tauntT:0,counterT:0,counterReduction:.5,counterPower:0,paralyzeNext:0,paralyzeAction:0,nextDamagePenalty:0,actionDamagePenalty:0,symphonyGuard:0,symphonySourceKey:null,singleTauntSource:null,singleTauntDamagePenalty:0,nextSpeedPenalty:0,divineMark:0,celestialBlind:0,divineRayCount:0,monkKi:null,monkGuardNext:0,voidSeals:c.id==='voidknight'?5:0}}\n\nfunction effectiveSpeed(f){return Math.round(f.speed+(f.speedBuffT>0?f.speedBuff:0)-(f.nextSpeedPenalty||0))}\n\nfunction effectiveMaxHp(f){return Math.max(1,Math.round(f.maxHp*(f.hpSealT>0?.5:1)))}\n\nfunction dotCount(f,type){return (f[type+'Stacks']||[]).length}\n\nfunction getFighter(side,index){return (side==='player'?myTeam:botTeam)[index]||null}\n\nfunction getPlanKey(f){return `${f.side}-${f.index}`}\n\nfunction availableSkills(f){return ['skill1','skill2','skill3'].filter((k,i)=>f['cd'+(i+1)]<=0)}\n\nfunction actionPriority(p){\n const f=getFighter(p.actor.side,p.actor.index);\n if(!f)return 0;\n if(f.id==='guardian'&&(p.skill==='skill2'||p.skill==='skill3'))return 3;\n if(f.id==='valkyrie'&&p.skill==='skill3')return 3;\n if(f.id==='augment'&&p.skill==='skill3')return 2.5;\n if(f.id==='bard'&&p.skill==='skill3')return 2.5;\n if(f.id==='priest'&&p.skill==='skill2')return 2;\n return 1;\n}\n\nfunction consumeFrozenAction(plan,actor){\n if(!plan||!actor)return 0;\n const skill=actor[plan.skill];if(!skill)return 0;\n const n=Number(plan.skill.slice(-1));\n actor['cd'+n]=Math.max(actor['cd'+n]||0,skill.cd+1);\n // L'action a bien été dépensée : les effets « prochaine action » sont eux aussi consommés.\n actor.nextDamagePenalty=0;\n if(actor.id==='monk'&&actor.monkKi){actor.monkKi=null;}\n if(skill.target==='enemy'){\n  actor.paralyzeNext=0;\n  if(actor.singleTauntSource){actor.singleTauntSource=null;actor.singleTauntDamagePenalty=0;}\n }\n spendVoidSeal(actor,'Gel absolu');\n return skill.cd;\n}\n\nfunction targetOf(plan){return plan.target?getFighter(plan.target.side,plan.target.index):null}\n\nfunction armorDamage(target,raw){const effective=Math.max(0,target.armor-target.armorBreak);let dmg=raw*(100/(100+effective*.55));if(target.drT>0)dmg*=1-target.dr;return dmg}\n\nfunction heal(target,amount,source=null){if(!target||!target.alive)return 0;const poisonPenalty=Math.min(.35,dotCount(target,'poison')*.05);const anti=target.antiHealT>0?target.antiHeal:0;const mult=Math.max(.10,1-anti-poisonPenalty),val=amount*mult,before=target.hp;target.hp=Math.min(effectiveMaxHp(target),target.hp+val);const actual=Math.max(0,target.hp-before);recordHealing(source||currentStatsActor||target,target,actual);floatText(target,'+'+Math.round(val),'heal');if((anti>0||poisonPenalty>0)&&val>0)logBattle(`${target.name} reçoit ${Math.round(val)} soin (${Math.round((1-mult)*100)} % de réduction).`, 'effect');return val}\n\nfunction consumeDivineMarkCrit(target,crit){\n if(target&&target.divineMark>0){target.divineMark=0;floatText(target,'CRIT DIVIN','crit');logBattle(`<b>${target.name}</b> consomme sa Marque divine : le coup devient critique.`, 'effect');return true}\n return crit;\n}\n\nfunction damage(att,target,raw,crit=false,isAoE=false){\n if(!target||!target.alive)return 0;\n if(target.guardBy&&target.guardT>0&&!isAoE){\n  const guard=getFighter(target.guardBy.side,target.guardBy.index);\n  if(guard&&guard.alive&&guard!==target){\n   logBattle(`<b>${guard.name}</b> intercepte l'attaque destinée à <b>${target.name}</b>.`, 'effect');\n   target=guard;raw*=0.78;\n  }else{target.guardBy=null;target.guardT=0}\n }\n if(target.dodgeNext&&!isAoE){target.dodgeNext=false;floatText(target,'ESQUIVE','miss');logBattle(`<b>${target.name}</b> esquive le sort ciblé.`, 'effect');return 0}\n if(target.halfNextTargeted&&!isAoE){target.halfNextTargeted=false;raw*=.5;floatText(target,'-50%','heal');logBattle(`<b>${target.name}</b> réduit de moitié les dégâts de la prochaine attaque ciblée grâce à Disparition toxique.`, 'effect');}\n if(target.monkGuardNext>0&&!isAoE){const r=Math.max(0,Math.min(.9,target.monkGuardNext));target.monkGuardNext=0;raw*=1-r;floatText(target,`-${Math.round(r*100)}%`,'heal');logBattle(`<b>${target.name}</b> canalise le Ki de la Grue : le prochain coup ciblé est réduit de ${Math.round(r*100)} %.`, 'effect');}\n const actMult=(att&&att.dmgBuffT>0?Math.max(.2,1+att.dmgBuff):1)*(att&&att.paralyzeAction?0.5:1)*(att&&att.actionDamagePenalty>0?Math.max(.2,1-att.actionDamagePenalty):1);\n if(target.symphonyGuard>0&&!isAoE){\n  let prevented=armorDamage(target,raw*actMult);if(crit)prevented*=1.65;\n  target.symphonyGuard=0;\n  const source=fighterByStatKey(target.symphonySourceKey),before=target.hp;\n  heal(target,prevented*.60,source);\n  const actual=Math.max(0,target.hp-before);\n  floatText(target,'CONCERT','heal');\n  logBattle(`<b>Concert symphonique</b> protège <b>${target.name}</b> : ${Math.round(prevented)} dégâts annulés et +${Math.round(actual)} PV réellement récupérés.`, 'effect');\n  return 0;\n }\n crit=consumeDivineMarkCrit(target,crit);\n const hadCounter=target.counterT>0&&!isAoE;\n if(hadCounter)raw*=Math.max(.1,1-(target.counterReduction||.5));\n let dmg=armorDamage(target,raw*actMult);\n if(crit)dmg*=1.65;\n if(target.shield>0){const block=Math.min(target.shield,dmg);target.shield-=block;dmg-=block}\n if(dmg<=.5){floatText(target,'BLOQUÉ','heal');if(target.reflectT>0&&target.reflectNext>0){target.reflectNext=0;target.reflectT=0}return 0}\n const applied=Math.min(dmg,Math.max(0,target.hp));target.hp-=dmg;recordDamage(att,target,applied);hitAnim(target);floatText(target,Math.round(dmg),crit?'crit':'');\n if(hadCounter&&att&&att.alive&&target.alive){\n  target.counterT=0;\n  let back=armorDamage(att,(target.counterPower||180)*(target.dmgBuffT>0?Math.max(.2,1+target.dmgBuff):1));\n  if(att.shield>0){const block=Math.min(att.shield,back);att.shield-=block;back-=block}\n  if(back>0){const appliedBack=Math.min(back,Math.max(0,att.hp));att.hp-=back;recordDamage(target,att,appliedBack);hitAnim(att);floatText(att,Math.round(back),'crit');logBattle(`<b>${target.name}</b> déclenche son contre et inflige ${Math.round(back)} dégâts à <b>${att.name}</b>.`, 'effect');if(att.hp<=0){att.hp=0;att.alive=false;logBattle(`<b>${att.name}</b> est éliminé.`, 'ko')}}\n }\n if(target.reflectT>0&&target.reflectNext>0&&att&&att.alive&&!isAoE){\n  let back=armorDamage(att,dmg*target.reflectNext);\n  if(att.shield>0){const block=Math.min(att.shield,back);att.shield-=block;back-=block}\n  target.reflectNext=0;target.reflectT=0;\n  if(back>0){const appliedBack=Math.min(back,Math.max(0,att.hp));att.hp-=back;recordDamage(target,att,appliedBack);hitAnim(att);floatText(att,Math.round(back),'crit');logBattle(`<b>${target.name}</b> renvoie ${Math.round(back)} dégâts à <b>${att.name}</b>.`, 'effect');if(att.hp<=0){att.hp=0;att.alive=false;logBattle(`<b>${att.name}</b> est éliminé.`, 'ko')}}\n }\n if(att&&att.lifestealT>0)heal(att,dmg*att.lifesteal);\n if(target.hp<=0){target.hp=0;target.alive=false;logBattle(`<b>${target.name}</b> est éliminé.`, 'ko')}\n return dmg}\n\nfunction damageShieldPierce(att,target,raw,ignoreShieldFrac=.5,crit=false,isAoE=false){\n if(!target||!target.alive)return 0;\n if(target.guardBy&&target.guardT>0&&!isAoE){\n  const guard=getFighter(target.guardBy.side,target.guardBy.index);\n  if(guard&&guard.alive&&guard!==target){\n   logBattle(`<b>${guard.name}</b> intercepte l'attaque destinée à <b>${target.name}</b>.`, 'effect');\n   target=guard;raw*=0.78;\n  }else{target.guardBy=null;target.guardT=0}\n }\n if(target.dodgeNext&&!isAoE){target.dodgeNext=false;floatText(target,'ESQUIVE','miss');logBattle(`<b>${target.name}</b> esquive le sort ciblé.`, 'effect');return 0}\n if(target.halfNextTargeted&&!isAoE){target.halfNextTargeted=false;raw*=.5;floatText(target,'-50%','heal');logBattle(`<b>${target.name}</b> réduit de moitié les dégâts de la prochaine attaque ciblée grâce à Disparition toxique.`, 'effect');}\n if(target.monkGuardNext>0&&!isAoE){const r=Math.max(0,Math.min(.9,target.monkGuardNext));target.monkGuardNext=0;raw*=1-r;floatText(target,`-${Math.round(r*100)}%`,'heal');logBattle(`<b>${target.name}</b> canalise le Ki de la Grue : le prochain coup ciblé est réduit de ${Math.round(r*100)} %.`, 'effect');}\n const actMult=(att&&att.dmgBuffT>0?Math.max(.2,1+att.dmgBuff):1)*(att&&att.paralyzeAction?0.5:1)*(att&&att.actionDamagePenalty>0?Math.max(.2,1-att.actionDamagePenalty):1);\n if(target.symphonyGuard>0&&!isAoE){\n  let prevented=armorDamage(target,raw*actMult);if(crit)prevented*=1.65;\n  target.symphonyGuard=0;\n  const source=fighterByStatKey(target.symphonySourceKey),before=target.hp;\n  heal(target,prevented*.60,source);\n  const actual=Math.max(0,target.hp-before);\n  floatText(target,'CONCERT','heal');\n  logBattle(`<b>Concert symphonique</b> protège <b>${target.name}</b> : ${Math.round(prevented)} dégâts annulés et +${Math.round(actual)} PV réellement récupérés.`, 'effect');\n  return 0;\n }\n crit=consumeDivineMarkCrit(target,crit);\n const hadCounter=target.counterT>0&&!isAoE;\n if(hadCounter)raw*=Math.max(.1,1-(target.counterReduction||.5));\n let dmg=armorDamage(target,raw*actMult);\n if(crit)dmg*=1.65;\n if(target.shield>0){\n  const blockCap=Math.max(0,target.shield*(1-ignoreShieldFrac));\n  const block=Math.min(blockCap,dmg);\n  target.shield-=block;dmg-=block;\n }\n if(dmg<=.5){floatText(target,'BLOQUÉ','heal');if(target.reflectT>0&&target.reflectNext>0){target.reflectNext=0;target.reflectT=0}return 0}\n const applied=Math.min(dmg,Math.max(0,target.hp));target.hp-=dmg;recordDamage(att,target,applied);hitAnim(target);floatText(target,Math.round(dmg),crit?'crit':'');\n if(hadCounter&&att&&att.alive&&target.alive){\n  target.counterT=0;\n  let back=armorDamage(att,(target.counterPower||180)*(target.dmgBuffT>0?Math.max(.2,1+target.dmgBuff):1));\n  if(att.shield>0){const block=Math.min(att.shield,back);att.shield-=block;back-=block}\n  if(back>0){const appliedBack=Math.min(back,Math.max(0,att.hp));att.hp-=back;recordDamage(target,att,appliedBack);hitAnim(att);floatText(att,Math.round(back),'crit');logBattle(`<b>${target.name}</b> déclenche son contre et inflige ${Math.round(back)} dégâts à <b>${att.name}</b>.`, 'effect');if(att.hp<=0){att.hp=0;att.alive=false;logBattle(`<b>${att.name}</b> est éliminé.`, 'ko')}}\n }\n if(target.reflectT>0&&target.reflectNext>0&&att&&att.alive&&!isAoE){\n  let back=armorDamage(att,dmg*target.reflectNext);\n  if(att.shield>0){const block=Math.min(att.shield,back);att.shield-=block;back-=block}\n  target.reflectNext=0;target.reflectT=0;\n  if(back>0){const appliedBack=Math.min(back,Math.max(0,att.hp));att.hp-=back;recordDamage(target,att,appliedBack);hitAnim(att);floatText(att,Math.round(back),'crit');logBattle(`<b>${target.name}</b> renvoie ${Math.round(back)} dégâts à <b>${att.name}</b>.`, 'effect');if(att.hp<=0){att.hp=0;att.alive=false;logBattle(`<b>${att.name}</b> est éliminé.`, 'ko')}}\n }\n if(att&&att.lifestealT>0)heal(att,dmg*att.lifesteal);\n if(target.hp<=0){target.hp=0;target.alive=false;logBattle(`<b>${target.name}</b> est éliminé.`, 'ko')}\n return dmg}\n\nfunction addDot(target,type,dmg,turns,count=1){const sourceKey=currentStatsActor?statKey(currentStatsActor):null;for(let i=0;i<count;i++)target[type+'Stacks'].push({dmg,turns,sourceKey})}\n\nfunction consumeDots(target,type){const arr=target[type+'Stacks'];const n=arr.length;target[type+'Stacks']=[];return n}\n\nfunction removeOneEachDot(target){['poison','disease','burn','bleed'].forEach(t=>{const a=target[t+'Stacks'];if(a.length)a.shift()})}\n\nfunction hasNegativeEffect(f){\n return !!(f.silencedRound||f.paralyzeNext||f.antiHealT>0||(f.dmgBuffT>0&&f.dmgBuff<0)||(f.speedBuffT>0&&f.speedBuff<0)||\n  (f.armorBreakT>0&&f.armorBreak>0)||f.hpSealT>0||f.tauntT>0||f.nextDamagePenalty>0||(f.nextSpeedPenalty||0)>0||!!f.singleTauntSource||\n  f.divineMark>0||f.celestialBlind>0||dotCount(f,'poison')||dotCount(f,'disease')||dotCount(f,'burn')||dotCount(f,'bleed'));\n}\n\nfunction cleanseOneNegative(f){\n if(f.celestialBlind>0){f.celestialBlind=0;return 'Éblouissement céleste'}\n if(f.divineMark>0){f.divineMark=0;return 'Marque divine'}\n if(f.silencedRound){f.silencedRound=false;return 'Silence'}\n if(f.paralyzeNext){f.paralyzeNext=0;return 'Paralysie'}\n if(f.antiHealT>0){f.antiHeal=0;f.antiHealT=0;return 'Anti-soin'}\n if(f.nextDamagePenalty>0){f.nextDamagePenalty=0;return 'Réduction de dégâts'}\n if((f.nextSpeedPenalty||0)>0){f.nextSpeedPenalty=0;return 'Réduction de Vitesse'}\n if(f.singleTauntSource){f.singleTauntSource=null;f.singleTauntDamagePenalty=0;return 'Défi glacé'}\n if(f.dmgBuffT>0&&f.dmgBuff<0){f.dmgBuff=0;f.dmgBuffT=0;return 'Réduction de dégâts'}\n if(f.speedBuffT>0&&f.speedBuff<0){f.speedBuff=0;f.speedBuffT=0;return 'Réduction de Vitesse'}\n if(f.armorBreakT>0&&f.armorBreak>0){f.armorBreak=0;f.armorBreakT=0;return 'Armure brisée'}\n if(f.hpSealT>0){f.hpSealT=0;return 'Scellement de PV'}\n if(f.tauntT>0&&f.tauntSource){f.tauntT=0;f.tauntSource=null;return 'Provocation'}\n if(dotCount(f,'poison')){f.poisonStacks=[];return 'Poison'}\n if(dotCount(f,'disease')){f.diseaseStacks=[];return 'Maladie'}\n if(dotCount(f,'burn')){f.burnStacks=[];return 'Brûlure'}\n if(dotCount(f,'bleed')){f.bleedStacks=[];return 'Saignement'}\n return null;\n}\n\nfunction spendVoidSeal(actor,reason=''){\n if(!actor||actor.id!=='voidknight'||!actor.alive)return;\n const before=Number(actor.voidSeals||0);\n if(before<=0)return;\n actor.voidSeals=before-1;\n const remaining=actor.voidSeals>0?Array.from({length:actor.voidSeals},()=> '●').join(' '):'aucun';\n logBattle(`<b>${actor.name}</b> consume 1 sceau du Néant${reason?` (${reason})`:''}. Restants : ${remaining}.`, 'effect');\n floatText(actor,`SCEAUX ${actor.voidSeals}/5`,'miss');\n if(actor.voidSeals<=0){\n  actor.hp=0;actor.shield=0;actor.alive=false;\n  logBattle(`<b>${actor.name}</b> a brisé son 5e sceau : sa compétence se résout, puis il meurt immédiatement.`, 'ko');\n  floatText(actor,'CONDAMNÉ','crit');\n }\n}\n\nfunction executePlan(plan){\n const actor=getFighter(plan.actor.side,plan.actor.index);if(!actor||!actor.alive)return;\n const key=plan.skill,skill=actor[key],enemyTeam=actor.side==='player'?botTeam:myTeam,allyTeam=actor.side==='player'?myTeam:botTeam,n=Number(key.slice(-1));\n let target=targetOf(plan);\n actor['cd'+n]=skill.cd+1;anim(actor,key);\n if(skill.target==='enemy'&&actor.singleTauntSource){const forced=getFighter(actor.singleTauntSource.side,actor.singleTauntSource.index);if(forced&&forced.alive)target=forced;actor.actionDamagePenalty=Math.max(actor.actionDamagePenalty||0,actor.singleTauntDamagePenalty||0);actor.singleTauntSource=null;actor.singleTauntDamagePenalty=0;}\n if(skill.target==='enemy'&&actor.tauntT>0&&actor.tauntSource){const forced=getFighter(actor.tauntSource.side,actor.tauntSource.index);if(forced&&forced.alive)target=forced;}\n if(actor.celestialBlind>0&&skill.target!=='self'){const pool=[...myTeam,...botTeam].filter(x=>x.alive);if(pool.length){target=pool[Math.floor(Math.random()*pool.length)];actor.celestialBlind=0;floatText(actor,'ÉBLOUI','miss');logBattle(`<b>${actor.name}</b> est ébloui : <b>${skill.name}</b> change de cible au hasard et vise <b>${target.name}</b>.`, 'effect');}}\n if(skill.target==='enemy'&&(!target||!target.alive)){logBattle(`<b>${actor.name}</b> perd son action : cible déjà éliminée.`, 'effect');spendVoidSeal(actor,'action consommée');return}\n if(skill.target==='ally'&&(!target||!target.alive)){logBattle(`<b>${actor.name}</b> perd son action : allié déjà éliminé.`, 'effect');spendVoidSeal(actor,'action consommée');return}\n currentStatsActor=actor;let direct=0;\n actor.actionDamagePenalty=Math.max(actor.actionDamagePenalty||0,actor.nextDamagePenalty||0);\n actor.nextDamagePenalty=0;\n actor.paralyzeAction=actor.paralyzeNext?1:0;if(actor.paralyzeAction){actor.paralyzeNext=0;floatText(actor,'PARALYSÉ','miss');logBattle(`<b>${actor.name}</b> agit sous paralysie : ses dégâts directs sont réduits de 50 % sur cette action.`, 'effect');}\n if(actor.id==='dragon'){\n  if(key==='skill1'){\n   direct=damage(actor,target,135);target.armorBreak=Math.min(18,target.armorBreak+6);target.armorBreakT=Math.max(target.armorBreakT,2);\n   logBattle(`<b>${actor.name}</b> — Griffe fracturante → <b>${target.name}</b> : ${Math.round(direct)} dégâts, Armure -${target.armorBreak}.`, 'action');\n  }else if(key==='skill2'){\n   flash('fire');direct=damage(actor,target,150,false,false);addDot(target,'burn',26,3,1);const other=enemyTeam.find(x=>x.alive&&x!==target);let splash=0;if(other)splash=damage(actor,other,80,false,true);\n   logBattle(`<b>${actor.name}</b> — Souffle écarlate : ${Math.round(direct)} sur ${target.name}${other?`, ${Math.round(splash)} sur ${other.name}`:''} + Brûlure x1.`, 'action');\n  }else{\n   flash('fire');actor.dmgBuff=.30;actor.dmgBuffT=3;actor.speedBuff=10;actor.speedBuffT=3;actor.dr=.20;actor.drT=3;heal(actor,50);\n   logBattle(`<b>${actor.name}</b> — Forme Dragon : +30 % dégâts, +10 Vitesse, -20 % dégâts reçus pendant 3 manches.`, 'effect');\n  }\n }\n else if(actor.id==='rogue'){\n  if(key==='skill1'){\n   flash('poison');direct=damage(actor,target,105,actor.nextCrit);actor.nextCrit=false;const extra=dotCount(target,'poison')>0?2:1;addDot(target,'poison',28,3,extra);\n   logBattle(`<b>${actor.name}</b> — Lame infectée → <b>${target.name}</b> : ${Math.round(direct)} dégâts + Poison x${extra}.`, 'action');\n  }else if(key==='skill2'){\n   actor.halfNextTargeted=true;actor.nextCrit=true;actor.speedBuff=25;actor.speedBuffT=2;enemyTeam.filter(x=>x.alive).forEach(e=>addDot(e,'poison',28,3,1));\n   flash('poison');logBattle(`<b>${actor.name}</b> — Disparition toxique : prochaine attaque critique, prochaine attaque ciblée subie réduite de 50 %, +25 Vitesse et Poison x1 sur chaque ennemi vivant.`, 'effect');\n  }else{\n   flash('poison');const stacks=consumeDots(target,'poison');direct=damage(actor,target,130+stacks*45,actor.nextCrit);actor.nextCrit=false;target.antiHeal=Math.max(target.antiHeal,.60);target.antiHealT=Math.max(target.antiHealT,2);addDot(target,'poison',24,2,2);\n   logBattle(`<b>${actor.name}</b> — Exécution toxique → <b>${target.name}</b> : ${Math.round(direct)} dégâts, ${stacks} Poison(s) consommé(s), anti-soin 60 % et Poison x2 réappliqué.`, 'action');\n  }\n }\n else if(actor.id==='wolf'){\n  if(key==='skill1'){\n   flash('blood');direct=damage(actor,target,145);addDot(target,'bleed',30,3,1);heal(actor,direct*.10);\n   logBattle(`<b>${actor.name}</b> — Morsure ouverte → <b>${target.name}</b> : ${Math.round(direct)} dégâts + Saignement x1.`, 'action');\n  }else if(key==='skill2'){\n   flash('blood');actor.dmgBuff=.35;actor.dmgBuffT=2;actor.speedBuff=20;actor.speedBuffT=2;actor.lifesteal=.25;actor.lifestealT=2;\n   logBattle(`<b>${actor.name}</b> — Frénésie sanguinaire : +35 % dégâts, +20 Vitesse et 25 % vol de vie pendant 2 manches.`, 'effect');\n  }else{\n   flash('blood');const stacks=consumeDots(target,'bleed');let raw=180+stacks*55;if(target.hp/target.maxHp<.35)raw*=1.10;direct=damage(actor,target,raw,true);heal(actor,stacks*10);\n   logBattle(`<b>${actor.name}</b> — Éventration → <b>${target.name}</b> : ${Math.round(direct)} dégâts critiques, ${stacks} Saignement(s) consommé(s).`, 'action');\n  }\n }\n else if(actor.id==='sniper'){\n  if(key==='skill1'){\n   flash('holy');let raw=115;if(target.armorBreakT>0&&target.armorBreak>0)raw+=25;direct=damage(actor,target,raw,false,false);\n   logBattle(`<b>${actor.name}</b> — Tir précis → <b>${target.name}</b> : ${Math.round(direct)} dégâts${raw>115?' (bonus sur armure brisée)':''}.`, 'action');\n  }else if(key==='skill2'){\n   flash('holy');direct=damage(actor,target,210,false,false);target.armorBreak=Math.min(24,target.armorBreak+7);target.armorBreakT=Math.max(target.armorBreakT,2);\n   logBattle(`<b>${actor.name}</b> — Tir perforant → <b>${target.name}</b> : ${Math.round(direct)} dégâts, Armure -${target.armorBreak}.`, 'action');\n  }else{\n   flash('holy');let raw=325;if(target.hp/target.maxHp>.70)raw*=1.18;direct=damage(actor,target,raw,true,false);\n   logBattle(`<b>${actor.name}</b> — Balle de jugement → <b>${target.name}</b> : ${Math.round(direct)} dégâts critiques.`, 'action');\n  }\n }\n else if(actor.id==='shadow'){\n  if(key==='skill1'){\n   flash('shadow');direct=damage(actor,target,125,false,false);heal(actor,direct*.45);\n   logBattle(`<b>${actor.name}</b> — Toucher nécrotique → <b>${target.name}</b> : ${Math.round(direct)} dégâts, vol de vie 45 %.`, 'action');\n  }else if(key==='skill2'){\n   flash('shadow');actor.dmgBuff=.35;actor.dmgBuffT=2;actor.speedBuff=25;actor.speedBuffT=2;actor.lifesteal=.50;actor.lifestealT=2;\n   logBattle(`<b>${actor.name}</b> — Pacte d’ombre : +35 % dégâts, +25 Vitesse et 50 % vol de vie pendant 2 manches.`, 'effect');\n  }else{\n   flash('shadow');direct=damage(actor,target,210,false,false);const other=enemyTeam.find(x=>x.alive&&x!==target);let splash=0;if(other)splash=damage(actor,other,120,false,true);const drained=heal(actor,direct+splash);actor.shield+=100;\n   logBattle(`<b>${actor.name}</b> — Voile de la mort : ${Math.round(direct)} sur ${target.name}${other?`, ${Math.round(splash)} sur ${other.name}`:''}, puis +${Math.round(drained)} PV et 70 Bouclier.`, 'action');\n  }\n }\n\n else if(actor.id==='augment'){\n  if(key==='skill1'){\n   flash('shadow');const bonus=Math.min(140,actor.arcaneCharge*35);direct=damage(actor,target,125+bonus,false,false);actor.arcaneCharge=Math.min(4,actor.arcaneCharge+1);\n   logBattle(`<b>${actor.name}</b> — Orbe cumulative → <b>${target.name}</b> : ${Math.round(direct)} dégâts. Puissance accumulée : +${actor.arcaneCharge*35}.`, 'action');\n  }else if(key==='skill2'){\n   flash('shadow');target.dmgBuff=Math.max(target.dmgBuff,.60);target.dmgBuffT=Math.max(target.dmgBuffT,2);target.speedBuff=Math.max(target.speedBuff,28);target.speedBuffT=Math.max(target.speedBuffT,2);target.shield+=90;\n   logBattle(`<b>${actor.name}</b> — Accélération runique : <b>${target.name}</b> gagne +60 % dégâts, +28 Vitesse pendant 2 manches et 90 Bouclier.`, 'effect');\n  }else{\n   flash('shadow');const beforeHp=target.hp;const lost=Math.max(1,Math.floor(beforeHp*.35));target.hp=Math.max(0,beforeHp-lost);recordDamage(actor,target,beforeHp-target.hp);floatText(target,Math.round(beforeHp-target.hp),'crit');\n   logBattle(`<b>${actor.name}</b> — Scellement miroir → <b>${target.name}</b> : ${Math.round(beforeHp-target.hp)} PV actuels scellés (35 %, ignore armure et bouclier).`, 'action');\n   if(target.hp<=0&&target.alive){target.hp=0;target.alive=false;logBattle(`<b>${target.name}</b> est éliminé.`, 'ko')}\n  }\n }\n else if(actor.id==='dragonette'){\n  if(key==='skill1'){\n   flash('fire');direct=damage(actor,target,105,false,false);addDot(target,'burn',24,3,1);\n   logBattle(`<b>${actor.name}</b> — Flamme draconique → <b>${target.name}</b> : ${Math.round(direct)} dégâts + Brûlure x2.`, 'action');\n  }else if(key==='skill2'){\n   flash('holy');const healed=heal(target,135);const burnTarget=enemyTeam.filter(x=>x.alive).sort((a,b)=>a.hp/effectiveMaxHp(a)-b.hp/effectiveMaxHp(b))[0];if(burnTarget)addDot(burnTarget,'burn',22,3,1);\n   logBattle(`<b>${actor.name}</b> — Braise curative : <b>${target.name}</b> récupère +${Math.round(healed)} PV${burnTarget?` et <b>${burnTarget.name}</b> subit Brûlure x1`:''}.`, 'action');\n  }else{\n   flash('fire');addDot(target,'burn',27,3,4);const ally=allyTeam.filter(x=>x.alive).sort((a,b)=>a.hp/effectiveMaxHp(a)-b.hp/effectiveMaxHp(b))[0]||actor;const healed=heal(ally,250);\n   logBattle(`<b>${actor.name}</b> — Inferno vital : <b>${target.name}</b> reçoit Brûlure x4 et <b>${ally.name}</b> récupère +${Math.round(healed)} PV.`, 'action');\n  }\n }\n else if(actor.id==='plague'){\n  if(key==='skill1'){\n   flash('disease');direct=damage(actor,target,80,false,false);addDot(target,'disease',24,3,2);target.antiHeal=Math.max(target.antiHeal,.35);target.antiHealT=Math.max(target.antiHealT,2);\n   logBattle(`<b>${actor.name}</b> — Peste hématophage → <b>${target.name}</b> : ${Math.round(direct)} dégâts, Maladie x2 et anti-soin 35 %.`, 'action');\n  }else if(key==='skill2'){\n   flash('disease');direct=damage(actor,target,100,false,false);addDot(target,'disease',26,3,2);target.speedBuff=Math.min(target.speedBuff,-15);target.speedBuffT=Math.max(target.speedBuffT,2);\n   logBattle(`<b>${actor.name}</b> — Fièvre flétrissante → <b>${target.name}</b> : ${Math.round(direct)} dégâts, Maladie x2 et Vitesse -15.`, 'action');\n  }else{\n   flash('disease');direct=damage(actor,target,120,false,false);addDot(target,'disease',28,3,2);target.antiHeal=Math.max(target.antiHeal,.45);target.antiHealT=Math.max(target.antiHealT,3);target.speedBuff=Math.min(target.speedBuff,-18);target.speedBuffT=Math.max(target.speedBuffT,3);target.dmgBuff=Math.min(target.dmgBuff,-.18);target.dmgBuffT=Math.max(target.dmgBuffT,3);\n   logBattle(`<b>${actor.name}</b> — Pandémie terminale → <b>${target.name}</b> : ${Math.round(direct)} dégâts, Attaque -18 %, anti-soin 45 %, Vitesse -18 et Maladie x2.`, 'action');\n  }\n }\n else if(actor.id==='dice'){\n  if(key==='skill1'){\n   flash('blood');const roll=1+Math.floor(Math.random()*6);const raw=[0,90,110,135,165,210,270][roll];direct=damage(actor,target,raw,false,false);\n   logBattle(`<b>${actor.name}</b> — Lancer de dés → <b>${target.name}</b> : dé ${roll}, ${Math.round(direct)} dégâts.`, 'action');\n  }else if(key==='skill2'){\n   flash('blood');const crit=Math.random()<.55;const raw=crit?215:145;direct=damage(actor,target,raw,crit,false);if(crit)addDot(target,'bleed',34,3,2);\n   logBattle(`<b>${actor.name}</b> — Jackpot sanglant → <b>${target.name}</b> : ${Math.round(direct)} dégâts${crit?' critiques + Saignement x2':' (pas de critique)'} .`, 'action');\n  }else{\n   flash('blood');const oneshot=Math.random()<.10;direct=damage(actor,target,oneshot?3000:220,oneshot,false);\n   logBattle(`<b>${actor.name}</b> — Tout ou rien → <b>${target.name}</b> : ${oneshot?'JACKPOT ! ':''}${Math.round(direct)} dégâts${oneshot?' (exécution chanceuse)':' .'}`, 'action');\n  }\n }\n\n else if(actor.id==='guardian'){\n\n  if(key==='skill1'){\n   flash('holy');direct=damage(actor,target,95,false,false);const other=enemyTeam.find(x=>x.alive&&x!==target);let splash=0;if(other)splash=damage(actor,other,95,false,true);\n   logBattle(`<b>${actor.name}</b> — Entaille circulaire : ${Math.round(direct)} sur ${target.name}${other?`, ${Math.round(splash)} sur ${other.name}`:''}.`, 'action');\n  }else if(key==='skill2'){\n   flash('holy');target.guardBy={side:actor.side,index:actor.index};target.guardT=Math.max(target.guardT,2);target.shield+=80;\n   logBattle(`<b>${actor.name}</b> — Mur protecteur : <b>${target.name}</b> sera protégé de la prochaine attaque ciblée et gagne 80 Bouclier.`, 'effect');\n  }else{\n   flash('holy');actor.reflectNext=1.5;actor.reflectT=2;actor.shield+=60;\n   logBattle(`<b>${actor.name}</b> — Rempart miroir : la prochaine attaque ciblée contre lui sera renvoyée à 150 % et il gagne 60 Bouclier.`, 'effect');\n  }\n }\n else if(actor.id==='berserker'){\n  const missing=Math.max(0,actor.maxHp-actor.hp),missingPct=Math.max(0,1-actor.hp/actor.maxHp);\n  if(key==='skill1'){\n   flash('blood');direct=damage(actor,target,95,false,false);const healed=heal(actor,35+direct*.12);\n   logBattle(`<b>${actor.name}</b> — Frappe vorace → <b>${target.name}</b> : ${Math.round(direct)} dégâts et +${Math.round(healed)} PV.`, 'action');\n  }else if(key==='skill2'){\n   flash('blood');let raw=135*(1+missingPct*.70);direct=damage(actor,target,raw,false,false);addDot(target,'bleed',18,2,1);\n   logBattle(`<b>${actor.name}</b> — Rage du manque → <b>${target.name}</b> : ${Math.round(direct)} dégâts${missingPct>0?` (bonus ${Math.round(missingPct*70)} %)`:''} + Saignement x1.`, 'action');\n  }else{\n   flash('blood');let raw=Math.min(650,Math.max(120,missing));direct=damage(actor,target,raw,false,false);\n   logBattle(`<b>${actor.name}</b> — Dette de sang → <b>${target.name}</b> : ${Math.round(direct)} dégâts basés sur les PV manquants (${Math.round(missing)}).`, 'action');\n  }\n }\n\n\n else if(actor.id==='monk'){\n  const consumedKi=actor.monkKi||null;\n  let raw=key==='skill1'?120:key==='skill2'?145:195;\n  if(consumedKi==='offense'&&target&&target.alive){\n   target.armorBreak=Math.max(target.armorBreak||0,5);target.armorBreakT=Math.max(target.armorBreakT||0,2);\n   raw*=1.20;\n   logBattle(`<b>Ki du Tigre</b> : Armure -5 appliquée avant l’impact sur <b>${target.name}</b> et dégâts de la technique +20 %.`, 'effect');\n  }\n  if(consumedKi==='defense'&&actor.alive){\n   actor.shield+=50;actor.monkGuardNext=Math.max(actor.monkGuardNext||0,.20);\n   floatText(actor,'KI GRUE','heal');\n   logBattle(`<b>${actor.name}</b> consomme le Ki de la Grue : +50 Bouclier et -20 % sur le prochain coup ciblé reçu.`, 'effect');\n  }\n  if(consumedKi==='control'&&actor.alive){\n   const missing=Math.max(0,effectiveMaxHp(actor)-actor.hp),beforeHeal=actor.hp;\n   heal(actor,60+missing*.10,actor);\n   const healed=Math.max(0,actor.hp-beforeHeal);\n   if(target&&target.alive)target.nextDamagePenalty=Math.max(target.nextDamagePenalty||0,.20);\n   logBattle(`<b>Ki du Serpent</b> : <b>${actor.name}</b> récupère ${Math.round(healed)} PV et <b>${target.name}</b> infligera 20 % de dégâts directs en moins sur sa prochaine action.`, 'effect');\n  }\n  flash(key==='skill3'?'holy':'blood');\n  direct=damage(actor,target,raw,false,false);\n  const nextKi=key==='skill1'?'offense':key==='skill2'?'defense':'control';\n  actor.monkKi=nextKi;\n  const kiName=nextKi==='offense'?'Tigre':nextKi==='defense'?'Grue':'Serpent';\n  const bonusText=consumedKi==='offense'?' • Ki du Tigre consommé':consumedKi==='defense'?' • Ki de la Grue consommé':consumedKi==='control'?' • Ki du Serpent consommé':'';\n  logBattle(`<b>${actor.name}</b> — <b>${skill.name}</b> → <b>${target.name}</b> : ${Math.round(direct)} dégâts${bonusText}. <b>Ki du ${kiName}</b> préparé pour la prochaine compétence.`, 'action');\n  floatText(actor,`KI ${kiName.toUpperCase()}`,'heal');\n }\n\n else if(actor.id==='archangel'){\n  if(key==='skill1'){\n   flash('holy');direct=damage(actor,target,120,false,false);if(target.alive)target.divineMark=1;\n   logBattle(`<b>${actor.name}</b> — Marque du Jugement → <b>${target.name}</b> : ${Math.round(direct)} dégâts${target.alive?' et Marque divine appliquée':''}.`, 'action');\n  }else if(key==='skill2'){\n   flash('holy');direct=damage(actor,target,95,false,false);if(target.alive)target.celestialBlind=1;\n   logBattle(`<b>${actor.name}</b> — Éblouissement céleste → <b>${target.name}</b> : ${Math.round(direct)} dégâts${target.alive?' ; sa prochaine compétence ciblée visera un combattant aléatoire':''}.`, 'action');\n  }else{\n   actor.divineRayCount=(actor.divineRayCount||0)+1;\n   if(actor.divineRayCount>=3){\n    divineJudgmentFX(target);const before=Math.max(0,target.hp);target.hp=0;target.shield=0;target.alive=false;recordDamage(actor,target,before);floatText(target,'JUGEMENT','crit');actor.divineRayCount=0;\n    logBattle(`<b>${actor.name}</b> — <b>ŒIL DIVIN : JUGEMENT</b> → <b>${target.name}</b> est éliminé instantanément. Le compteur repart à 0/3.`, 'ko');\n   }else{\n    flash('holy');direct=damage(actor,target,175,false,false);\n    logBattle(`<b>${actor.name}</b> — Rayon du Jugement → <b>${target.name}</b> : ${Math.round(direct)} dégâts. Œil divin ${actor.divineRayCount}/3.`, 'action');\n   }\n  }\n }\n else if(actor.id==='voidknight'){\n  if(key==='skill1'){\n   flash('shadow');direct=damage(actor,target,210,false,false);\n   logBattle(`<b>${actor.name}</b> — Entaille du Néant → <b>${target.name}</b> : ${Math.round(direct)} dégâts.`, 'action');\n  }else if(key==='skill2'){\n   flash('shadow');direct=damageShieldPierce(actor,target,340,.5,false,false);\n   logBattle(`<b>${actor.name}</b> — Fracture dimensionnelle → <b>${target.name}</b> : ${Math.round(direct)} dégâts et 50 % du Bouclier est ignoré.`, 'action');\n  }else{\n   flash('shadow');direct=damage(actor,target,520,false,false);\n   logBattle(`<b>${actor.name}</b> — Exécution du Néant → <b>${target.name}</b> : ${Math.round(direct)} dégâts.`, 'action');\n  }\n }\n else if(actor.id==='paladin'){\n  const missing=Math.max(0,actor.maxHp-actor.hp);\n  if(key==='skill1'){\n   flash('holy');direct=damage(actor,target,150,false,false);const gained=Math.round(direct*.55);actor.shield+=gained;\n   logBattle(`<b>${actor.name}</b> — Lame sacrée → <b>${target.name}</b> : ${Math.round(direct)} dégâts et ${gained} Bouclier.`, 'action');\n  }else if(key==='skill2'){\n   flash('holy');const healAmt=90+missing*.34;const healed=heal(actor,healAmt);actor.shield+=40;\n   logBattle(`<b>${actor.name}</b> — Prière de rempart : récupère +${Math.round(healed)} PV et gagne 40 Bouclier.`, 'effect');\n  }else{\n   flash('holy');enemyTeam.filter(x=>x.alive).forEach(e=>{e.tauntSource={side:actor.side,index:actor.index};e.tauntT=Math.max(e.tauntT,2)});actor.shield+=80;\n   logBattle(`<b>${actor.name}</b> — Défi rayonnant : les ennemis sont forcés de l’attaquer pendant 2 manches.`, 'effect');\n  }\n }\n else if(actor.id==='samurai'){\n  if(key==='skill1'){\n   flash('shadow');const missingPct=Math.max(0,1-target.hp/effectiveMaxHp(target));const raw=95+missingPct*150;direct=damage(actor,target,raw,true,false);\n   logBattle(`<b>${actor.name}</b> — Lame du crépuscule → <b>${target.name}</b> : ${Math.round(direct)} dégâts${missingPct>0?` (bonus ${Math.round(missingPct*100)} % cible blessée)`:''}.`, 'action');\n  }else if(key==='skill2'){\n   flash('shadow');actor.counterT=1;actor.counterReduction=.5;actor.counterPower=210;\n   logBattle(`<b>${actor.name}</b> — Contre lunaire : la prochaine attaque ciblée subie cette manche est réduite de moitié puis contre-attaquée.`, 'effect');\n  }else{\n   flash('shadow');let hits=[];for(let i=0;i<3;i++){const living=enemyTeam.filter(x=>x.alive);if(!living.length)break;const victim=living[Math.floor(Math.random()*living.length)];const raw=Math.max(85,effectiveMaxHp(victim)*0.12);const dealt=damage(actor,victim,raw,false,false);hits.push(`${victim.name} ${Math.round(dealt)}`)}\n   logBattle(`<b>${actor.name}</b> — Déferlante des ombres : ${hits.join(' • ')}.`, 'action');\n  }\n }\n\n else if(actor.id==='shaman'){\n  if(key==='skill1'){\n   flash('holy');direct=damage(actor,target,100,false,false);const para=Math.random()<.15;if(para)target.paralyzeNext=1;\n   logBattle(`<b>${actor.name}</b> — Éclair ancestral → <b>${target.name}</b> : ${Math.round(direct)} dégâts${para?' et paralysie (attaque suivante affaiblie)':''}.`, 'action');\n  }else if(key==='skill2'){\n   flash('fire');direct=damage(actor,target,105,true,false);addDot(target,'burn',16,3,1);\n   logBattle(`<b>${actor.name}</b> — Orbe de magma → <b>${target.name}</b> : ${Math.round(direct)} dégâts critiques + Brûlure x1.`, 'action');\n  }else{\n   const reports=[];flash('holy');\n   for(let i=0;i<3;i++){\n    const living=enemyTeam.filter(x=>x.alive);if(!living.length)break;\n    const victim=living[Math.floor(Math.random()*living.length)];const dealt=damage(actor,victim,65,false,false);const para=Math.random()<.15;if(para)victim.paralyzeNext=1;\n    reports.push(`Éclair sur ${victim.name} ${Math.round(dealt)}${para?' + Paralysie':''}`)\n   }\n   for(let i=0;i<2;i++){\n    const living=enemyTeam.filter(x=>x.alive);if(!living.length)break;\n    const victim=living[Math.floor(Math.random()*living.length)];const dealt=damage(actor,victim,76,true,false);addDot(victim,'burn',16,3,1);\n    reports.push(`Magma sur ${victim.name} ${Math.round(dealt)} + Brûlure`)\n   }\n   logBattle(`<b>${actor.name}</b> — Maelstrom : ${reports.join(' • ')}.`, 'action');\n  }\n }\n\n else if(actor.id==='valkyrie'){\n  if(key==='skill1'){\n   flash('holy');direct=damage(actor,target,105,false,false);target.nextSpeedPenalty=Math.max(target.nextSpeedPenalty||0,30);\n   logBattle(`<b>${actor.name}</b> — Coup de bouclier → <b>${target.name}</b> : ${Math.round(direct)} dégâts et Vitesse -30 pour sa prochaine action.`, 'action');\n  }else if(key==='skill2'){\n   flash('holy');target.singleTauntSource={side:actor.side,index:actor.index};target.singleTauntDamagePenalty=Math.max(target.singleTauntDamagePenalty||0,.25);\n   logBattle(`<b>${actor.name}</b> — Défi glacé → <b>${target.name}</b> : sa prochaine compétence offensive devra cibler la Valkyrie et infligera 25 % de dégâts en moins.`, 'action');\n  }else{\n   flash('ice');roundFreezeSourceKey=statKey(actor);showAbsoluteFreeze(roundFreezeSourceKey);\n   logBattle(`<b>${actor.name}</b> — Gel absolu : tous les autres combattants sont gelés. Leurs actions validées sont annulées pour cette manche.`, 'action');\n  }\n }\n else if(actor.id==='bard'){\n  if(key==='skill1'){\n   flash('holy');direct=damage(actor,target,90,false,false);target.nextDamagePenalty=Math.max(target.nextDamagePenalty||0,.25);\n   logBattle(`<b>${actor.name}</b> — Note percutante → <b>${target.name}</b> : ${Math.round(direct)} dégâts. Sa prochaine action infligera 25 % de dégâts directs en moins.`, 'action');\n  }else if(key==='skill2'){\n   flash('holy');target.shield+=100;const removed=cleanseOneNegative(target);\n   logBattle(`<b>${actor.name}</b> — Hymne protecteur : <b>${target.name}</b> gagne 100 Bouclier${removed?` et est libéré de <b>${removed}</b>`:' ; aucun effet négatif à retirer'}.`, 'effect');\n  }else{\n   flash('holy');allyTeam.filter(x=>x.alive).forEach(a=>{a.symphonyGuard=1;a.symphonySourceKey=statKey(actor)});\n   logBattle(`<b>${actor.name}</b> — Concert symphonique : cette manche, le premier coup direct reçu par chaque allié est annulé et 60 % des dégâts prévus sont convertis en soins.`, 'action');\n  }\n }\n else if(actor.id==='priest'){\n\n  if(key==='skill1'){\n   flash('holy');direct=damage(actor,target,125);const lowest=allyTeam.filter(x=>x.alive).sort((a,b)=>a.hp/a.maxHp-b.hp/b.maxHp)[0]||actor;const healed=heal(lowest,70);\n   logBattle(`<b>${actor.name}</b> — Éclat rédempteur → <b>${target.name}</b> : ${Math.round(direct)} dégâts, et <b>${lowest.name}</b> récupère +${Math.round(healed)} PV.`, 'action');\n  }else if(key==='skill2'){\n   flash('holy');target.silencedRound=true;\n   logBattle(`<b>${actor.name}</b> — Silence sacré → <b>${target.name}</b> : cible réduite au silence.`, 'action');\n  }else{\n   flash('holy');allyTeam.filter(x=>x.alive).forEach(a=>{a.sanctuaryT=Math.max(a.sanctuaryT,2);a.sanctuaryPower=Math.max(a.sanctuaryPower,Math.round(a.maxHp*.12));a.sanctuarySourceKey=statKey(actor)});enemyTeam.filter(x=>x.alive).forEach(e=>{e.antiHeal=Math.max(e.antiHeal,.50);e.antiHealT=Math.max(e.antiHealT,3)});\n   logBattle(`<b>${actor.name}</b> — Liturgie d’abstinence : l’équipe alliée récupère 12 % PV max par tour pendant 2 tours et les soins ennemis sont réduits de 50 % pendant 3 tours.`, 'action');\n  }\n }\n spendVoidSeal(actor);\n actor.paralyzeAction=0;actor.actionDamagePenalty=0;currentStatsActor=null;\n updateUI();\n}\n\nfunction applyEndRoundEffects(){\n const living=[...myTeam,...botTeam].filter(f=>f.alive);\n const hadEffects=living.some(f=>dotCount(f,'poison')||dotCount(f,'disease')||dotCount(f,'burn')||dotCount(f,'bleed')||f.sanctuaryT>0);\n if(hadEffects)logBattle(`FIN DE MANCHE ${round} — effets cumulés`, 'round');\n living.forEach(f=>{\n  ['poison','disease','burn','bleed'].forEach(type=>{\n   const arr=f[type+'Stacks'];if(!arr.length)return;\n   const stacks=arr.length,total=arr.reduce((s,x)=>s+x.dmg,0);let dmg=total;\n   if(f.shield>0){const block=Math.min(f.shield,dmg);f.shield-=block;dmg-=block;}\n   if(dmg>0){const hpBefore=f.hp,applied=Math.min(dmg,Math.max(0,hpBefore));f.hp-=dmg;const ratio=total>0?dmg/total:0;arr.forEach(x=>recordDamageByKey(x.sourceKey,f,total>0?applied*(x.dmg/total):0));floatText(f,Math.round(dmg),type);logBattle(`<b>${f.name}</b> subit ${Math.round(dmg)} dégâts de ${type==='poison'?'Poison':type==='disease'?'Maladie':type==='burn'?'Brûlure':'Saignement'} x${stacks}.`, 'dot');}\n   else logBattle(`<b>${f.name}</b> absorbe les dégâts de ${type==='poison'?'Poison':type==='disease'?'Maladie':type==='burn'?'Brûlure':'Saignement'} x${stacks} avec son bouclier.`, 'dot');\n   arr.forEach(x=>x.turns--);f[type+'Stacks']=arr.filter(x=>x.turns>0);\n  });\n  if(f.sanctuaryT>0){const healed=heal(f,f.sanctuaryPower||55,fighterByStatKey(f.sanctuarySourceKey));logBattle(`<b>${f.name}</b> récupère ${Math.round(healed)} PV grâce à la liturgie.`, 'effect');f.sanctuaryT--;if(f.sanctuaryT<=0){f.sanctuaryPower=0;f.sanctuarySourceKey=null;}}\n  ['cd1','cd2','cd3'].forEach(k=>{if(f[k]>0)f[k]--});\n  if(f.armorBreakT>0){f.armorBreakT--;if(f.armorBreakT<=0)f.armorBreak=0;}\n  if(f.antiHealT>0){f.antiHealT--;if(f.antiHealT<=0)f.antiHeal=0;}if(f.hpSealT>0)f.hpSealT--;\n  if(f.drT>0)f.drT--;if(f.dmgBuffT>0)f.dmgBuffT--;if(f.speedBuffT>0)f.speedBuffT--;\n  if(f.lifestealT>0){f.lifestealT--;if(f.lifestealT<=0)f.lifesteal=0;}\n  if(f.guardT>0){f.guardT--;if(f.guardT<=0)f.guardBy=null;}\n  if(f.reflectT>0){f.reflectT--;if(f.reflectT<=0)f.reflectNext=0;}\n  if(f.counterT>0){f.counterT--;if(f.counterT<=0)f.counterPower=0;}\n  if(f.tauntT>0){f.tauntT--;if(f.tauntT<=0)f.tauntSource=null;}\n  f.symphonyGuard=0;f.symphonySourceKey=null;\n  if(f.hp<=0&&f.alive){f.hp=0;f.alive=false;logBattle(`<b>${f.name}</b> succombe aux effets cumulés.`, 'ko');}\n });\n updateUI();\n}\nfunction initEngine(team1,team2){\n myTeam=team1.map((id,i)=>makeFighter(id,'player',i));botTeam=team2.map((id,i)=>makeFighter(id,'enemy',i));\n round=1;phase='planning';ended=false;roundFreezeSourceKey=null;currentStatsActor=null;matchLog=[];matchStats={};\n [...myTeam,...botTeam].forEach(f=>{matchStats[statKey(f)]={key:statKey(f),id:f.id,name:f.name,color:f.color,side:f.side,damage:0,healing:0,taken:0}});return publicSnapshot();\n}\nfunction clone(v){return JSON.parse(JSON.stringify(v))}\nfunction publicSnapshot(){return {round,phase,ended,roundFreezeSourceKey,teams:{1:clone(myTeam),2:clone(botTeam)},stats:clone(matchStats)}}\nfunction canonicalSide(slot,localSide){if(slot===1)return localSide==='player'?'player':'enemy';return localSide==='player'?'enemy':'player'}\nfunction canonicalPlan(slot,p){if(!p||!p.actor)return null;const actorSide=slot===1?'player':'enemy';const actor={side:actorSide,index:Number(p.actor.index)};let target=null;if(p.target)target={side:canonicalSide(slot,String(p.target.side||'')),index:Number(p.target.index)};return {side:actorSide,actor,skill:String(p.skill||''),target}}\nfunction validateCanonicalPlan(slot,p){\n if(!p||!p.actor)return {ok:false,error:'Plan incomplet'};const side=slot===1?'player':'enemy';\n if(p.actor.side!==side||![0,1].includes(p.actor.index))return {ok:false,error:'Combattant invalide'};const actor=getFighter(side,p.actor.index);if(!actor||!actor.alive)return {ok:false,error:'Combattant indisponible'};\n if(!['skill1','skill2','skill3'].includes(p.skill)||!actor[p.skill])return {ok:false,error:'Compétence invalide'};const n=Number(p.skill.slice(-1));if((actor['cd'+n]||0)>0)return {ok:false,error:'Compétence en recharge'};\n const kind=actor[p.skill].target;if(kind==='self')p.target={side,index:actor.index};else{if(!p.target||![0,1].includes(Number(p.target.index)))return {ok:false,error:'Cible invalide'};const wanted=kind==='ally'?side:(side==='player'?'enemy':'player');if(p.target.side!==wanted)return {ok:false,error:'Mauvais camp de cible'};const tar=getFighter(p.target.side,p.target.index);if(!tar||!tar.alive)return {ok:false,error:'Cible indisponible'}}\n return {ok:true,plan:p};\n}\nfunction validatePlans(slot,rawPlans){\n const side=slot===1?'player':'enemy',living=(side==='player'?myTeam:botTeam).filter(f=>f.alive);const arr=Array.isArray(rawPlans)?rawPlans:Object.values(rawPlans||{}),out=[],seen=new Set();\n for(const raw of arr){const cp=canonicalPlan(slot,raw);if(!cp)continue;const key=cp.actor.index;if(seen.has(key))return {ok:false,error:'Deux actions pour le même combattant'};const v=validateCanonicalPlan(slot,cp);if(!v.ok)return v;seen.add(key);out.push(v.plan)}\n if(out.length!==living.length)return {ok:false,error:'Il faut valider une action par combattant vivant'};for(const f of living)if(!seen.has(f.index))return {ok:false,error:'Action manquante'};return {ok:true,plans:out};\n}\nfunction fallbackPlans(slot){const side=slot===1?'player':'enemy',team=side==='player'?myTeam:botTeam,foes=side==='player'?botTeam:myTeam,out=[];team.filter(f=>f.alive).forEach(f=>{const key=availableSkills(f)[0]||'skill1',kind=f[key].target;let target=null;if(kind==='self')target={side,index:f.index};else if(kind==='ally')target={side,index:f.index};else{const e=foes.find(x=>x.alive);if(e)target={side:e.side,index:e.index}}out.push({side,actor:{side,index:f.index},skill:key,target})});return out}\nfunction winnerSlot(){const a=myTeam.some(x=>x.alive),b=botTeam.some(x=>x.alive);if(!a&&!b)return 0;if(a&&!b)return 1;if(!a&&b)return 2;return null}\nfunction consumeLogs(from){return clone(matchLog.slice(from))}\nfunction resolveRound(slot1Plans,slot2Plans){\n phase='resolution';roundFreezeSourceKey=null;[...myTeam,...botTeam].forEach(f=>f.silencedRound=false);const p1=slot1Plans&&slot1Plans.length?slot1Plans:fallbackPlans(1),p2=slot2Plans&&slot2Plans.length?slot2Plans:fallbackPlans(2);\n const actions=[...p1,...p2].filter(Boolean).map(p=>{const f=getFighter(p.actor.side,p.actor.index);return {...clone(p),speed:f?effectiveSpeed(f):0,usedSpeedPenalty:f?(f.nextSpeedPenalty||0):0,priority:actionPriority(p),tie:Math.random()}}).sort((a,b)=>b.priority-a.priority||b.speed-a.speed||b.tie-a.tie);\n actions.forEach(a=>{if(a.usedSpeedPenalty>0){const f=getFighter(a.actor.side,a.actor.index);if(f)f.nextSpeedPenalty=0}});const events=[];\n for(const p of actions){if(ended)break;const actor=getFighter(p.actor.side,p.actor.index);const before=matchLog.length;if(!actor||!actor.alive){events.push({kind:'skip',plan:clone(p),logs:[],state:publicSnapshot()});continue}\n   if(roundFreezeSourceKey&&statKey(actor)!==roundFreezeSourceKey){const frozenCd=consumeFrozenAction(p,actor),skill=actor[p.skill];logBattle('<b>'+actor.name+'</b> est gelé : <b>'+(skill?skill.name:'action')+'</b> est annulée mais considérée comme utilisée'+(frozenCd>0?' — CD '+frozenCd:'')+'.','effect');events.push({kind:'action',plan:clone(p),logs:consumeLogs(before),state:publicSnapshot()});continue}\n   if(actor.silencedRound){logBattle('<b>'+actor.name+'</b> est réduit au silence : action annulée.','effect');events.push({kind:'action',plan:clone(p),logs:consumeLogs(before),state:publicSnapshot()});continue}\n   executePlan(p);const w=winnerSlot();if(w!==null)ended=true;events.push({kind:'action',plan:clone(p),logs:consumeLogs(before),state:publicSnapshot()});\n }\n let w=winnerSlot();if(!ended&&w===null){const before=matchLog.length;applyEndRoundEffects();w=winnerSlot();if(w!==null)ended=true;events.push({kind:'end_round',logs:consumeLogs(before),state:publicSnapshot()})}\n if(!ended){round++;phase='planning';roundFreezeSourceKey=null;[...myTeam,...botTeam].forEach(f=>f.silencedRound=false)}else phase='end';const final=publicSnapshot();final.phase=phase;final.ended=ended;final.winnerSlot=winnerSlot();return {round:final.round,events,finalState:final,winnerSlot:final.winnerSlot,ended};\n}\nfunction getRound(){return round} function getState(){return publicSnapshot()}\nglobalThis.__engine={initEngine,validatePlans,fallbackPlans,resolveRound,getRound,getState};\n";
function createBattleEngine(team1,team2){const context=vm.createContext({console,Math,JSON});vm.runInContext(ENGINE_SOURCE,context,{timeout:1000});context.__engine.initEngine(team1,team2);return {validatePlans:(slot,plans)=>context.__engine.validatePlans(slot,plans),fallbackPlans:(slot)=>context.__engine.fallbackPlans(slot),resolveRound:(p1,p2)=>context.__engine.resolveRound(p1,p2),getState:()=>context.__engine.getState(),getRound:()=>context.__engine.getRound()}}
module.exports={createBattleEngine};
